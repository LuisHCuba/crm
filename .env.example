# Variáveis usadas no build (Vite embute no bundle).
VITE_HASURA_ADMIN_SECRET=trocar_pelo_admin_secret_do_hasura

# Geração de formulários com IA via n8n
# Path relativo ao proxy /n8n (dev) ou URL absoluta em produção
VITE_N8N_FORM_WEBHOOK_URL=/webhook/crm-form-ai
VITE_N8N_BASE_URL=https://flux.lhcx.tech
