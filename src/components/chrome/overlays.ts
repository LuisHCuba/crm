/** Acima do header (z-10) e do toggle da sidebar (z-200). */
export const OVERLAY_Z_CLASS = "z-[300]";

/** Padding seguro para modais/dropdowns não encostarem nas bordas da viewport. */
export const OVERLAY_SAFE_PADDING_CLASS = "p-4 sm:p-6";

/** Altura máxima de painéis flutuantes respeitando a viewport. */
export const OVERLAY_PANEL_MAX_H_CLASS = "max-h-[calc(100dvh-2rem)]";
